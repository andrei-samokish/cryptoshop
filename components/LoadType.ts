export enum LoadType {
  general = "general", // for generating cards in Main
  personal = "personal", // for generating cards in Personal Account
  searched = "searched", // for generating cards in Search
}
